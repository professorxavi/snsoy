import { ReadingSkeleton } from "@/components/layout";

/** A race page. Set in the reading column, with its traits as the outline. */
export default function Loading() {
  return <ReadingSkeleton />;
}
