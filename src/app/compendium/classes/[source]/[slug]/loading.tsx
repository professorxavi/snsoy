import { ReadingSkeleton } from "@/components/layout";

/**
 * A class page. The heaviest of the entity pages — the class, its table, every
 * feature it grants and every subclass — so it is the one most worth covering.
 */
export default function Loading() {
  return <ReadingSkeleton />;
}
